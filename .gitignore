__pycache__/
*.py[cod]
.DS_Store
.ipynb_checkpoints/
.venv/
venv/

# Raw and derived data are not redistributed (OpenAPS Data Commons terms).
# `artifacts/` is excluded whole: alongside the aggregate results it holds the
# archive's own wearer ids and per-person statistics. The publishable subset is
# copied into `demo_data/` with those ids pseudonymised.
data/
artifacts/
